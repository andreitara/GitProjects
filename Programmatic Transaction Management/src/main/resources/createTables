use SpringTest
GO
CREATE TABLE Car(
	 ID INT IDENTITY PRIMARY KEY,
	 Mark VARCHAR(30) NOT NULL,
	 Year INT NOT NULL,
	 Engine INT);
GO
CREATE TABLE Driver(
	ID INT IDENTITY,
    CarID INT NOT NULL,
	Name VARCHAR(30) NOT NULL);
GO